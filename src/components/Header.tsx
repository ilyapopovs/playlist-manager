import { RenderCount } from "@/components/RenderCount";

export const Header = () => {
  return (
    <header className="text-center font-bold">
      Cute Header here <RenderCount />
    </header>
  );
};
